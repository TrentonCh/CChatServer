{
  "name": "c-chat-web-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy + frontend for the C TLS chat server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
